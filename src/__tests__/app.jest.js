import React from 'react';
import App from '../App';
import { shallow, mount } from 'enzyme';

describe('the application', () => {
  it('should', () => {
    console.log(App);
  })
  // const app = shallow(<App/>)
  // it('should render', () => {
  //   expect(app.instance().doesSomething()).toBe(5);
  // });
});
